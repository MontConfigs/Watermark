local GuiService = game:GetService("GuiService")
local Studio = settings().Studio

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(1, 0, 1, 0)
mainFrame.BackgroundTransparency = 1
mainFrame.Parent = Studio

local tabFrame = Instance.new("Frame")
tabFrame.Size = UDim2.new(1, 0, 0.1, 0)
tabFrame.Parent = mainFrame

local sectionFrame = Instance.new("Frame")
sectionFrame.Size = UDim2.new(1, 0, 0.9, 0)
sectionFrame.Position = UDim2.new(0, 0, 0.1, 0)
sectionFrame.Parent = mainFrame

local watermark = Instance.new("TextLabel")
watermark.Text = "Mont config"
watermark.Size = UDim2.new(0.2, 0, 0.1, 0)
watermark.Position = UDim2.new(0.01, 0, 0.01, 0)
watermark.BackgroundTransparency = 1
watermark.Parent = tabFrame

local version = Instance.new("TextLabel")
version.Text = "Version 1.0"
version.Size = UDim2.new(0.2, 0, 0.1, 0)
version.Position = UDim2.new(0.01, 0, 0.01, 0)
version.BackgroundTransparency = 1
version.Parent = sectionFrame
